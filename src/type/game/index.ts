export interface Game{
    file:File,
    teamNum:number,
    studentNum:number,
    perTeamNum:number,
    cid:number,
}

export interface GameList{
    gmtCreate:string
    status:number,
    studentCount:number,
    teamCount:number,
}